# API
DATABASE_URL="postgresql://spendwise:spendwise@localhost:5432/spendwise?schema=public"
JWT_SECRET="replace-with-a-strong-secret"
# Local API port. Cloud platforms usually provide PORT automatically.
API_PORT=4000
# Comma-separate multiple deployed web origins when needed.
CORS_ORIGIN="http://localhost:3000"

# Web
NEXT_PUBLIC_API_URL="http://localhost:4000/api"
SPENDWISE_DEMO_EMAIL="demo@spendwise.local"
SPENDWISE_DEMO_PASSWORD="SpendWise123"
# Optional production server-side token for rendering the dashboard without demo login.
SPENDWISE_API_TOKEN=""
